spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vyouyou/springcloudconfigserver
          search-paths: config
          username:
          password:
server:
  port: 7001
